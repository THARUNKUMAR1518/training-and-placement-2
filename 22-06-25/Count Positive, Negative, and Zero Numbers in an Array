public class CountNumbers {
    public static void main(String[] args) {
        int[] nums = {1, -3, 5, 0, -2, 0, 4};
        int pos = 0, neg = 0, zero = 0;

        for (int n : nums) {
            if (n > 0) pos++;
            else if (n < 0) neg++;
            else zero++;
        }

        System.out.println("Positive: " + pos);
        System.out.println("Negative: " + neg);
        System.out.println("Zero: " + zero);
    }
}

Output:

Positive: 3  
Negative: 2  
Zero: 2
